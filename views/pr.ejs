<%- include('layout', { title, body: `
<h1>Pull Request #${pr.id}</h1>
<p class="subtitle">Merge status and details.</p>

<div class="card">
  <p class="empty">PR details will appear here once webhooks are connected.</p>
</div>
` }) %>
